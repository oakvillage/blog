name: publish

on:
  push:
    inputs:
      name:
        description: 'deploy content'
        required: true
        default: 'default content'
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: git test
      run: |
        git status
        git branch