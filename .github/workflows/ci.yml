name: publish

on:
  workflow_dispatch:
    inputs:
      name:
        description: 'deploy content'
        required: true
        default: 'default content'
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: test
      run: |
        git status
        ls -la